<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsdlab.biseo.repository.UserRepository">
    <select id="findByEmail" resultType="String">
        SELECT account_id FROM User WHERE account_id = #{account_id} AND is_deleted = 0;
    </select>

    <select id="findById" resultType="UserModel">
        SELECT name, account_id, student_id, department, is_auth FROM User
        WHERE id = #{id} AND is_deleted = 0;
    </select>

    <insert id="signUp" parameterType="UserModel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO User(name, account_id, password, student_id, department, user_type)
        VALUES (#{name},#{account_id},#{password},#{student_id},#{department}, #{user_type})
    </insert>


</mapper>