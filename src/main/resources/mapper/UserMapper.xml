<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsdlab.biseo.repository.UserRepository">
    <select id="findByEmail" resultType="String">
        SELECT email FROM User WHERE email = #{email} AND is_deleted = 0;
    </select>

    <select id="findById" resultType="UserModel">
        SELECT name, email, student_id, department, is_auth FROM User
        WHERE id = #{id} AND is_deleted = 0;
    </select>

    <insert id="signUp" parameterType="UserModel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO User(name, email, password, student_id, department, user_type)
        VALUES (#{name},#{email},#{password},#{student_id},#{department}, #{user_type})
    </insert>


</mapper>