<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcsdlab.biseo.repository.NoticeRepository">
    <insert id="createNotice" parameterType="NoticeModel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Notice(user_id, title, content)
        VALUES (#{userId}, #{title}, #{content});
    </insert>

    <insert id="createTarget" parameterType="NoticeTargetModel" useGeneratedKeys="true">
        INSERT INTO Notice_Target(notice_id, target) VALUES (#{noticeId}, #{target})
    </insert>

    <resultMap id="noticeFileMap" type="NoticeFileModel">
        <result property="path" column="path"/>
        <result property="type" column="type"/>
    </resultMap>

    <resultMap id="noticeAndFileMap" type="NoticeAndFileModel">
        <id property="id" column="id"/>
        <result property="userId" column="userId"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <collection property="files" resultMap="noticeFileMap"/>
    </resultMap>

    <select id="findByNoticeId" resultMap="noticeAndFileMap">
        SELECT
            n.id,
            (SELECT name FROM USER WHERE id = n.user_id) AS userId,
            n.title,
            n.content,
            n.created_at,
            n.updated_at,
            f.path,
            f.type
        FROM
            Notice n LEFT JOIN Notice_File f
        ON
            n.id = f.notice_id
        WHERE
            n.id = #{noticeId};
    </select>
    
    <select id="findReadLogByUserId" resultType="NoticeReadModel">
        SELECT
            *
        FROM
            Notice_Read
        WHERE
            user_id = #{userId} AND notice_id = #{noticeId};
    </select>

    <insert id="createReadLog" parameterType="NoticeReadModel">
        INSERT INTO Notice_Read(notice_id, user_id) VALUES (#{noticeId}, #{userId});
    </insert>

    <select id="findReadLogByNoticeId" resultType="UserModel">
        SELECT
            id, name, department
        FROM
            User
        WHERE
            id IN (SELECT user_id FROM Notice_Read WHERE notice_id = #{noticeId})
    </select>

    <select id="findNotReadLogByNoticeId" resultType="UserModel">
        SELECT
            id, name, department
        FROM
            User
        WHERE
            department NOT IN (SELECT target FROM Notice_Target WHERE notice_id = #{noticeId})
                AND
            id NOT IN (SELECT user_id FROM Notice_Read WHERE notice_id = #{noticeId});
    </select>

    <select id="findTargetByNoticeId" resultType="Integer">
        SELECT target from Notice_Target WHERE notice_id = #{noticeId};
    </select>
</mapper>